# Self-Verification Engine Spec

See [[self_verification_spec.html|Full spec]] for interface contract and implementation steps.

**Core idea:** In-memory pre-flight check (AST, imports, contract) before the Verifier writes to disk.
