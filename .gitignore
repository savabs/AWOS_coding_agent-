# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/
ENV/
.env

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/
*.coveragerc

# IDEs
.vscode/settings.json
.idea/
*.swp
*.swo
*.swa
.DS_Store
Thumbs.db

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/

# Database / cache files
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
.cache/
*.cache

# NOT ignored: .awos/ is tracked deliberately. It holds the learning data the
# whole system compounds on — reward_store.jsonl, error_patterns.jsonl, skills
# and goals — not disposable scratch. Ignoring it would untrack that history.
# Cassettes recorded for the benchmark live under .awos/cassettes/ and are also
# meant to be committed, so a recorded comparison replays for free.

# Model artifacts (large files — store separately)
*.pkl
*.pickle
*.pt
*.pth
*.bin
*.onnx
*.h5
*.hdf5
models/weights/
checkpoints/weights/

# Data (raw data should not be in git)
data/raw/
data/external/

# Logs
*.log
logs/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Node (if any frontend)
node_modules/
.npm

# Obsidian workspace state (not configuration)
.obsidian/workspace
.obsidian/workspace.json
.obsidian/cache
.obsidian/plugins/*/data.json

# Checkpoint archive (large, historical — optional to include)
# docs/memory/archive/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Wedge fixture runtime (mission worktrees / local git)
tests/fixtures/wedge_v1/ci_rescue_sprint/.git/
tests/fixtures/wedge_v1/ci_rescue_sprint/.awos/
